# Backend API URL (no trailing slash)
# Leave empty for local development — the Vite proxy handles /api requests
# Set to your deployed backend URL when deploying to Vercel
VITE_API_URL=https://flovex-api.vercel.app
VITE_LOGO_DEV_TOKEN=